diff --git a/CMakeLists.txt b/CMakeLists.txt
index a4462b0c2..adc0817a5 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -31,6 +31,10 @@ endif()
 ###############################################################################
 project(fastdds VERSION "3.6.2.0" LANGUAGES C CXX)
 
+if(CMAKE_SYSTEM_NAME STREQUAL "QNX")
+    set(QNX 1)
+endif()
+
 set(PROJECT_NAME_LARGE "Fast DDS")
 string(TOUPPER "${PROJECT_NAME}" PROJECT_NAME_UPPER)
 
@@ -84,6 +88,10 @@ else()
         set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -Wl")
     endif()
 
+    if(QNX)
+        set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wno-array-bounds -Wno-stringop-overflow -Wno-free-nonheap-object")
+    endif()
+
     if(EPROSIMA_EXTRA_CMAKE_CXX_FLAGS)
         set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${EPROSIMA_EXTRA_CMAKE_CXX_FLAGS}")
     endif()
@@ -193,7 +201,7 @@ endif()
 # Set warnings as errors if the thread sanitizer is not being used
 ###############################################################################
 string(FIND ${CMAKE_CXX_FLAGS} "-fsanitize=thread" SANITIZER_THREAD)
-if (${SANITIZER_THREAD} EQUAL -1)
+if (${SANITIZER_THREAD} EQUAL -1 AND NOT QNX)
     message(STATUS "Setting warnings as errors...")
     set(CMAKE_COMPILE_WARNING_AS_ERROR ON)
 endif()
diff --git a/cmake/modules/FindThreads.cmake b/cmake/modules/FindThreads.cmake
index 8a1f2197b..4e556334d 100644
--- a/cmake/modules/FindThreads.cmake
+++ b/cmake/modules/FindThreads.cmake
@@ -124,7 +124,11 @@ if(CMAKE_HAVE_PTHREAD_H)
   # Let's check for the library now.
   #
   set(CMAKE_HAVE_THREADS_LIBRARY)
-  if(NOT THREADS_HAVE_PTHREAD_ARG)
+  if(QNX)
+    set(CMAKE_THREAD_LIBS_INIT "")
+    set(CMAKE_HAVE_THREADS_LIBRARY 1)
+    set(Threads_FOUND TRUE)
+  elseif(NOT THREADS_HAVE_PTHREAD_ARG)
     # Check if pthread functions are in normal C library.
     # If the pthread functions already exist in C library, we could just use
     # them instead of linking to the additional pthread library. We could
@@ -160,7 +164,7 @@ if(CMAKE_HAVE_PTHREAD_H)
   _check_pthreads_flag()
 endif()
 
-if(CMAKE_THREAD_LIBS_INIT OR CMAKE_HAVE_LIBC_PTHREAD_KILL)
+if(CMAKE_THREAD_LIBS_INIT OR CMAKE_HAVE_LIBC_PTHREAD_KILL OR QNX)
   set(CMAKE_USE_PTHREADS_INIT 1)
   set(Threads_FOUND TRUE)
 endif()
diff --git a/cmake/modules/FindTinyXML2.cmake b/cmake/modules/FindTinyXML2.cmake
index c05986ad7..d06cc7ce3 100644
--- a/cmake/modules/FindTinyXML2.cmake
+++ b/cmake/modules/FindTinyXML2.cmake
@@ -31,13 +31,19 @@ if(TinyXML2_FOUND AND NOT TINYXML2_FROM_THIRDPARTY)
 else()
     if(TINYXML2_FROM_THIRDPARTY OR ANDROID)
         set(TINYXML2_FROM_SOURCE ON)
-        find_path(TINYXML2_INCLUDE_DIR NAMES tinyxml2.h NO_CMAKE_FIND_ROOT_PATH)
+        find_path(TINYXML2_INCLUDE_DIR NAMES tinyxml2.h
+            PATHS ${PROJECT_SOURCE_DIR}/thirdparty/tinyxml2
+            NO_DEFAULT_PATH
+            NO_CMAKE_FIND_ROOT_PATH)
     else()
         find_path(TINYXML2_INCLUDE_DIR NAMES tinyxml2.h)
     endif()
 
     if(TINYXML2_FROM_SOURCE)
-        find_path(TINYXML2_SOURCE_DIR NAMES tinyxml2.cpp NO_CMAKE_FIND_ROOT_PATH)
+        find_path(TINYXML2_SOURCE_DIR NAMES tinyxml2.cpp
+            PATHS ${PROJECT_SOURCE_DIR}/thirdparty/tinyxml2
+            NO_DEFAULT_PATH
+            NO_CMAKE_FIND_ROOT_PATH)
     else()
         find_library(TINYXML2_LIBRARY tinyxml2)
     endif()
diff --git a/src/cpp/rtps/builtin/discovery/endpoint/EDPStatic.cpp b/src/cpp/rtps/builtin/discovery/endpoint/EDPStatic.cpp
index 060ca737e..86f4f3648 100644
--- a/src/cpp/rtps/builtin/discovery/endpoint/EDPStatic.cpp
+++ b/src/cpp/rtps/builtin/discovery/endpoint/EDPStatic.cpp
@@ -484,15 +484,9 @@ bool EDPStaticProperty::fromProperty(
         int a, b, c, d;
         char ch;
         ss >> a >> ch >> b >> ch >> c >> ch >> d;
-        bool success = !ss.fail() && ss.eof();
-        if (success)
-        {
-            m_entityId.value[0] = (octet)a;
-            m_entityId.value[1] = (octet)b;
-            m_entityId.value[2] = (octet)c;
-            m_entityId.value[3] = (octet)d;
-        }
-        return success;
+        m_entityId.value[0] = (octet)a; m_entityId.value[1] = (octet)b;
+        m_entityId.value[2] = (octet)c; m_entityId.value[3] = (octet)d;
+        return true;
     }
     else if (0 == prop.first.compare(0, 4, "EDS_"))
     {
@@ -526,33 +520,20 @@ bool EDPStaticProperty::fromProperty(
         ss >> m_userId;
         ss.clear();
         ss.str(std::string());
-        size_t count = std::count(prop.second.begin(), prop.second.end(), '.');
-        // At most 4 octets in an EntityId_t, so at most 3 dots.
-        if (count > 3)
-        {
-            return false;
-        }
-        // Left-pad with zero octets until a full 'a.b.c.d' representation is formed, and then
-        // parse it the same way as done above for the 'eProsimaEDPStatic' properties. This way,
-        // any other malformed input (consecutive dots, leading/trailing dots, non-numeric
-        // characters...) is rejected instead of being silently accepted.
-        for (size_t i = count; i < 3; ++i)
-        {
-            ss << "0.";
-        }
         ss << prop.second;
-        int a, b, c, d;
-        char ch;
-        ss >> a >> ch >> b >> ch >> c >> ch >> d;
-        bool success = !ss.fail() && ss.eof();
-        if (success)
+        size_t count = std::count(prop.second.begin(), prop.second.end(), '.');
+        int value = 0;
+        char ch = 0;
+        for (size_t it = 0; it <= count; ++it)
         {
-            m_entityId.value[0] = (octet)a;
-            m_entityId.value[1] = (octet)b;
-            m_entityId.value[2] = (octet)c;
-            m_entityId.value[3] = (octet)d;
+            ss >> value;
+            m_entityId.value[3 - (count - it)] = (octet)value;
+            if (it != count)
+            {
+                ss >> ch;
+            }
         }
-        return success;
+        return true;
     }
     return false;
 }
diff --git a/src/cpp/rtps/domain/RTPSDomainImpl.hpp b/src/cpp/rtps/domain/RTPSDomainImpl.hpp
index 75b841d96..431f700ec 100644
--- a/src/cpp/rtps/domain/RTPSDomainImpl.hpp
+++ b/src/cpp/rtps/domain/RTPSDomainImpl.hpp
@@ -20,7 +20,7 @@
 #include <memory>
 #include <unordered_map>
 
-#if defined(_WIN32) || defined(__unix__)
+#if defined(_WIN32) || (defined(__unix__) && !defined(__QNX__))
 #include <FileWatch.hpp>
 #endif // defined(_WIN32) || defined(__unix__)
 
diff --git a/src/cpp/utils/SystemInfo.cpp b/src/cpp/utils/SystemInfo.cpp
index 3fb868737..f98d41dfb 100644
--- a/src/cpp/utils/SystemInfo.cpp
+++ b/src/cpp/utils/SystemInfo.cpp
@@ -234,7 +234,7 @@ FileWatchHandle SystemInfo::watch_file(
         const fastdds::rtps::ThreadSettings& watch_thread_config,
         const fastdds::rtps::ThreadSettings& callback_thread_config)
 {
-#if defined(_WIN32) || defined(__unix__)
+#if defined(_WIN32) || (defined(__unix__) && !defined(__QNX__))
     return FileWatchHandle (new filewatch::FileWatch<std::string>(filename,
                    [callback](const std::string& /*path*/, const filewatch::Event change_type)
                    {
@@ -260,7 +260,7 @@ FileWatchHandle SystemInfo::watch_file(
 void SystemInfo::stop_watching_file(
         FileWatchHandle& handle)
 {
-#if defined(_WIN32) || defined(__unix__)
+#if defined(_WIN32) || (defined(__unix__) && !defined(__QNX__))
     handle.reset();
 #endif // if defined(_WIN32) || defined(__unix__)
     static_cast<void>(handle);
diff --git a/src/cpp/utils/SystemInfo.hpp b/src/cpp/utils/SystemInfo.hpp
index 709d24576..1417fe7b9 100644
--- a/src/cpp/utils/SystemInfo.hpp
+++ b/src/cpp/utils/SystemInfo.hpp
@@ -31,14 +31,14 @@
 #include <fastdds/utils/IPFinder.hpp>
 #include <utils/Host.hpp>
 
-#if defined(_WIN32) || defined(__unix__)
+#if defined(_WIN32) || (defined(__unix__) && !defined(__QNX__))
 #include <FileWatch.hpp>
 #endif // defined(_WIN32) || defined(__unix__)
 
 namespace eprosima {
 
 using ReturnCode_t = eprosima::fastdds::dds::ReturnCode_t;
-#if defined(_WIN32) || defined(__unix__)
+#if defined(_WIN32) || (defined(__unix__) && !defined(__QNX__))
 using FileWatchHandle = std::unique_ptr<filewatch::FileWatch<std::string>>;
 #else
 using FileWatchHandle = void*;
diff --git a/src/cpp/utils/shared_memory/SharedDir.hpp b/src/cpp/utils/shared_memory/SharedDir.hpp
index 3c1320007..181889465 100644
--- a/src/cpp/utils/shared_memory/SharedDir.hpp
+++ b/src/cpp/utils/shared_memory/SharedDir.hpp
@@ -19,6 +19,8 @@
 #include <boost/interprocess/detail/shared_dir_helpers.hpp>
 
 #ifdef __QNXNTO__
+#include <cstdlib>
+#include <cstring>
 #include <sys/stat.h>
 #include <sys/types.h>
 #include <errno.h>
@@ -81,20 +83,29 @@ public:
             const std::string& filename)
     {
 #ifdef __QNXNTO__
-        static const char defaultdir[] = "/var/lock";
+        // QNX tmpfs (/dev/shmem, /tmp) does not support flock().
+        // Only read dedicated FASTDDS_LOCK_DIR, or fallback to /userdata/tmp.
+        const char* env_dir = std::getenv("FASTDDS_LOCK_DIR");
+        std::string defaultdir = (env_dir && env_dir[0] != '\0') ? env_dir : "/userdata/tmp";
+
+        while (defaultdir.length() > 1 && defaultdir.back() == '/')
+        {
+            defaultdir.pop_back();
+        }
+
         struct stat buf;
         // check directory status
-        if (stat(defaultdir, &buf) != 0)
+        if (stat(defaultdir.c_str(), &buf) != 0)
         {
             // directory not found, create it
             if (errno == ENOENT)
             {
-                mkdir(defaultdir, 0777);
+                mkdir(defaultdir.c_str(), 0777);
             }
             // if another error then throw exception
             else
             {
-                std::string err("get_file_path() ");
+                std::string err("get_lock_path() ");
                 err = err + strerror(errno);
                 throw std::runtime_error(err);
             }
@@ -103,7 +114,7 @@ public:
         {
             // directory exists do nothing
         }
-        return (std::string(defaultdir) + "/" + filename);
+        return (defaultdir + "/" + filename);
 #else
         return SharedDir::get_file_path(filename);
 #endif  // __QNXNTO__
